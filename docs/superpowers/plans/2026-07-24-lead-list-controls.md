# План улучшения списка заявок

**Цель:** ускорить поиск и разбор входящих заявок.

**Файлы:** `src/App.tsx`, `src/App.test.tsx`.

### 1. Поиск по телефону

- [ ] Добавить проверку поиска по номеру телефона в `src/App.test.tsx`.
- [ ] Убедиться, что проверка падает.
- [ ] Включить `phone` в существующую строку поиска и уточнить подсказку поля.
- [ ] Запустить `npm test -- src/App.test.tsx`.
- [ ] Закоммитить `feat: искать заявки по телефону`.

### 2. Счётчик результатов

- [ ] Добавить проверку текста с числом найденных заявок.
- [ ] Убедиться, что проверка падает.
- [ ] Вывести `visibleLeads.length` рядом с заголовком списка.
- [ ] Запустить `npm test -- src/App.test.tsx`.
- [ ] Закоммитить `feat: показать количество заявок`.

### 3. Фильтр без ответственного

- [ ] Добавить проверку фильтра заявок без ответственного.
- [ ] Убедиться, что проверка падает.
- [ ] Добавить переключатель и включить его в сброс фильтров.
- [ ] Запустить `npm test -- src/App.test.tsx`.
- [ ] Закоммитить `feat: отфильтровать заявки без ответственного`.
